/**
 * Shared Delivery Types
 * Common type definitions for delivery-related operations
 */

/**
 * Delivery status values
 */
export type DeliveryStatus = 'pending' | 'in_transit' | 'delayed' | 'delivered' | 'cancelled' | 'failed';
